# Runtime dependencies.
#
# Deliberately short.  smDA-Python needs imageio, tifffile, Pillow,
# scikit-image, opencv, matplotlib, plotly and numba as well; all of those
# belong to the viewer and the detector, neither of which is here.
#
# The Rust extension (smda_scan) is built from source; see README.
numpy>=1.26
scipy>=1.12
pandas>=2.2
streamlit>=1.40
